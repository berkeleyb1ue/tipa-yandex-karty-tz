
```mermaid
classDiagram
    class Пользователь {
        +String id
        +String имя
        +String email
        +String языкИнтерфейса
        +зарегистрироваться()
        +войти()
        +добавитьВИзбранное(точка)
        +настроитьПрофиль()
    }
 
    class Организация {
        +String id
        +String название
        +String категория
        +String часыРаботы
        +Число рейтинг
        +String меню
        +показатьКарточку()
    }
 
    class ТочкаНаКарте {
        +String id
        +Число широта
        +Число долгота
        +String адрес
    }
 
    class Отзыв {
        +String id
        +Число оценка
        +String текст
        +Дата дата
    }
 
    class Маршрут {
        +String id
        +ВидТранспорта тип
        +Число расстояние
        +Число времяВПути
        +построить(точкаА, точкаБ)
        +пересчитать()
        +выбратьВариант()
    }
 
    class ДорожноеСобытие {
        +String id
        +ТипСобытия тип
        +Дата время
        +отправить()
        +модерировать()
    }
 
    class ОбщественныйТранспорт {
        +String id
        +String номерМаршрута
        +String расписание
        +спрогнозироватьПрибытие()
    }
 
    class Уведомление {
        +String id
        +ТипУведомления тип
        +String текст
        +Дата время
        +отправить()
    }
 
    class ВидТранспорта {
        <<enumeration>>
        ПЕШКОМ
        АВТО
        ОБЩЕСТВЕННЫЙ
        ВЕЛО
    }
 
    class ТипСобытия {
        <<enumeration>>
        АВАРИЯ
        КАМЕРА
        РЕМОНТ
        ПЕРЕКРЫТИЕ
    }
 
    class ТипУведомления {
        <<enumeration>>
        PUSH
        IN_APP
    }
 
    Пользователь "1" --> "0..*" Маршрут : строит
    Пользователь "1" --> "0..*" Отзыв : оставляет
    Пользователь "1" --> "0..*" ДорожноеСобытие : сообщает
    Пользователь "1" --> "0..*" ТочкаНаКарте : избранное
    Пользователь "1" --> "0..*" Уведомление : получает
    Маршрут "1" --> "2..*" ТочкаНаКарте : проходит через
    Маршрут "0..1" --> "0..*" ОбщественныйТранспорт : использует
    Организация "1" --> "1" ТочкаНаКарте : расположена в
    Организация "1" --> "0..*" Отзыв : имеет
    ДорожноеСобытие "1" --> "0..*" Уведомление : генерирует
    Маршрут --> ВидТранспорта : тип
    ДорожноеСобытие --> ТипСобытия : тип
    Уведомление --> ТипУведомления : тип
    ```
